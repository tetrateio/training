type: "object"
required:
  - number
  - balance
  - owner
  - type
properties:
  number:
    type: "integer"
    format: "int64"
  balance:
    type: "number"
    format: "currency"
  owner:
    type: "string"
  type:
    type: string
    enum: [cash, saving]
