allOf:
  - $ref: "./newtransaction.yaml" # is relative to yaml that is being flattened
  - type: "object"
    required:
      - id
      - timestamp
    properties:
      id:
        type: string
      timestamp:
        type: "integer"
        format: "int64"
